╔══════════════════════════════════════════════════════════════════════════════╗
║                    LAB5 - CLUSTER EXECUTION CHEAT SHEET                      ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│ 1. INITIAL SETUP (once)                                                      │
└──────────────────────────────────────────────────────────────────────────────┘

ssh your_login@nash.ensimag.fr
cd ~/Bureau/FIB/cours/AVPR/Labs/LAB5
./setup_cluster.sh

┌──────────────────────────────────────────────────────────────────────────────┐
│ 2. SUBMIT JOBS                                                               │
└──────────────────────────────────────────────────────────────────────────────┘

Basic:
  ./run_cluster.sh                              # All tasks, default settings

Specific task:
  ./run_cluster.sh --task=1                     # Task 1 only
  ./run_cluster.sh --task=2                     # Task 2 only
  ./run_cluster.sh --task=3                     # Task 3 only

Custom resources:
  ./run_cluster.sh --mem=16GB --cpus=12 --time=8:00:00

Different GPU:
  ./run_cluster.sh --partition=a40              # NVIDIA A40 (46GB VRAM)
  ./run_cluster.sh --partition=v100             # Tesla V100 (32GB VRAM)
  ./run_cluster.sh --partition=rtx6000          # RTX 6000 (24GB VRAM, default)

┌──────────────────────────────────────────────────────────────────────────────┐
│ 3. MONITOR JOBS                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

Check status:
  ./check_status.sh                             # All your jobs
  ./check_status.sh <JOB_ID>                    # Specific job

SLURM commands:
  squeue -u $USER                               # Your jobs
  squeue -j <JOB_ID>                            # Specific job details

Follow logs:
  tail -f cluster_logs/output/*.out             # Standard output
  tail -f cluster_logs/errors/*.err             # Errors

Cancel:
  scancel <JOB_ID>                              # Cancel specific job
  scancel -u $USER                              # Cancel all your jobs

┌──────────────────────────────────────────────────────────────────────────────┐
│ 4. RETRIEVE RESULTS                                                          │
└──────────────────────────────────────────────────────────────────────────────┘

View results:
  ls -lh *.png *.pth                            # Generated files
  cat cluster_logs/output/*.out | grep -i accuracy

Copy to local machine:
  scp your_login@nash.ensimag.fr:~/Bureau/FIB/cours/AVPR/Labs/LAB5/*.png .

┌──────────────────────────────────────────────────────────────────────────────┐
│ 5. COMMON WORKFLOWS                                                          │
└──────────────────────────────────────────────────────────────────────────────┘

Quick test (1 hour):
  ./run_cluster.sh --task=1 --time=1:00:00 --mem=4GB

Full training (overnight):
  ./run_cluster.sh --task=all --time=12:00:00 --mem=16GB --cpus=12

High-performance:
  ./run_cluster.sh --task=all --partition=a40 --mem=24GB --time=8:00:00

┌──────────────────────────────────────────────────────────────────────────────┐
│ 6. TROUBLESHOOTING                                                           │
└──────────────────────────────────────────────────────────────────────────────┘

Test setup:
  ./test_setup.sh                               # Run all tests

Check environment:
  source lab5_env/bin/activate
  python3 -c "import torch; print(torch.__version__)"

View errors:
  cat cluster_logs/errors/*.err

Reinstall environment:
  rm -rf lab5_env
  ./setup_cluster.sh

┌──────────────────────────────────────────────────────────────────────────────┐
│ 7. JOB STATUS CODES                                                          │
└──────────────────────────────────────────────────────────────────────────────┘

PD  = Pending (waiting for resources)
R   = Running
CG  = Completing
CD  = Completed
F   = Failed
CA  = Cancelled

┌──────────────────────────────────────────────────────────────────────────────┐
│ 8. RESOURCE GUIDELINES                                                       │
└──────────────────────────────────────────────────────────────────────────────┘

MNIST (this project):
  Memory:  4-8GB
  CPUs:    4-8
  Time:    1-4 hours
  GPU:     Any (rtx6000 recommended)

Larger datasets:
  Memory:  16-32GB
  CPUs:    12-16
  Time:    8-24 hours
  GPU:     a40 (for 46GB VRAM)

┌──────────────────────────────────────────────────────────────────────────────┐
│ 9. USEFUL FILES                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

CLUSTER_README.md     - Quick start guide
CLUSTER_GUIDE.md      - Detailed documentation
setup_cluster.sh      - Environment setup
run_cluster.sh        - Job submission
check_status.sh       - Status monitoring
test_setup.sh         - Pre-flight checks

┌──────────────────────────────────────────────────────────────────────────────┐
│ 10. SUPPORT                                                                  │
└──────────────────────────────────────────────────────────────────────────────┘

Cluster issues:     support.info@ensimag.fr
Documentation:      See CLUSTER_GUIDE.md

╔══════════════════════════════════════════════════════════════════════════════╗
║                           Happy Computing! 🚀                                ║
╚══════════════════════════════════════════════════════════════════════════════╝

